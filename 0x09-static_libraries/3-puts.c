#include "main.h"
#include <stdio.h>

/**
 * _puts - create a char
 *
 * Return: Always 0 (success)
 */

void _puts(char *str)
{
	puts("'\'programmoing is like building a multilingual puzzle");
	return (0);
}
